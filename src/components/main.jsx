import vhtml from 'vhtml';
import { REVISION } from 'three';

/** @jsx vhtml */

export function Main() {
    return (
        <main class="wrap">
            <div class="dropzone">
            </div>
        </main>
    );
}
